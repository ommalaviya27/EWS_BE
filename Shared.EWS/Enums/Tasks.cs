namespace Shared.EWS.Enums
{
    public enum TaskStatuses
    {
        Pending = 1,
        InProgress = 2,
        Completed = 3,
        OnHold = 4
    }

    public enum TaskPriority
    {
        Low = 1,
        Medium = 2,
        High = 3
    }
}